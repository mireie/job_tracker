<%= form_with(model: event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
  <%= f.label :event %>
<%= f.text :event, class: "form-control"%>
<%= f.label %>
<%= f.date :date, class: "form-control"%>
<%= f.label %>
<%= f.text :contact_name, class: "form-control"%>
<%= f.label %>
<%= f.text :contact_title, class: "form-control"%>
<%= f.label %>
<%= f.date :follow_up_date, class: "form-control"%>
<%= f.label %>
<%= f.boolean :follow_up, class: "form-control"%>
    <%= form.submit %>
  </div>
<% end %>

