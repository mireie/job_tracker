<% if current_user %>
  <div class="row">
    <div class="col">
      <h2 class="display-3">Job Search Dashboard</h2>
      <hr>
      <div class="row">
        <div class="col">
          <h3>This Week</h3>
          <strong><%=pluralize(@jobs.this_week.count, 'application')%> this week!</strong>
          <% if @jobs.this_week.any? %>
            <%= render :partial => "jobs/job_grid", :locals => {:jobs => @jobs.this_week} %>
          <% else%>
            <p><%= link_to 'Get on it!', new_job_path %></p>
          <% end %>
        </div>
        <div class="col">
          <h3>Previous Week</h3>
          <strong><%=pluralize(@jobs.last_week.count, 'application')%> last week!</strong>
          <% if @jobs.last_week.any? %>
            <%= render :partial => "jobs/job_grid", :locals => {:jobs => @jobs.last_week} %>
          <% else%>
            <p><%= link_to 'Get on it!', new_job_path %></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-4">
      <h2 class="display-3">Add a Job</h2>
      <hr>
      <%= render 'form', job: @job = current_user.jobs.new() %>
    </div>
  </div>
  <hr>
  <h2>Active Applications</h2>
  <div class="row">
    <div class="col">
      <% if @jobs.any? %>
        <%= render :partial => "jobs/job_grid", :locals => {:jobs => @jobs.active} %>
      <% else%>
        <p>There are no jobs yet.</p>
      <% end %>
    </div>
  </div>
  <hr>
  <h2>Inactive Applications</h2>
  <div class="row">
    <div class="col">
      <% if @jobs.any? %>
        <%= render :partial => "jobs/job_grid", :locals => {:jobs => @jobs.inactive} %>
      <% else%>
        <p>There are no jobs yet.</p>
      <% end %>
    </div>
  </div>
<% else %>
  <p>Please <%= link_to "Sign up", '/users/sign_up' %> or <%= link_to "Sign in", '/users/sign_in'%> to continue.</p>
<% end %>
<br>
